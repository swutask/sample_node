'use strict';

/**
 * @type {{
 * activity: import('sequelize').ModelCtor,
 * announcement: import('sequelize').ModelCtor,
 * annotation: import('sequelize').ModelCtor,
 * applePayReceipt: import('sequelize').ModelCtor,
 * applePayWebhook: import('sequelize').ModelCtor,
 * appVersion: import('sequelize').ModelCtor,
 * attachment: import('sequelize').ModelCtor,
 * avatar: import('sequelize').ModelCtor,
 * betaTesters: import('sequelize').ModelCtor,
 * betaTests: import('sequelize').ModelCtor,
 * book: import('sequelize').ModelCtor,
 * bookActivity: import('sequelize').ModelCtor,
 * bookFolder: import('sequelize').ModelCtor,
 * bookLink: import('sequelize').ModelCtor,
 * bookOrder: import('sequelize').ModelCtor,
 * calendar: import('sequelize').ModelCtor,
 * chat: import('sequelize').ModelCtor,
 * chatSetting: import('sequelize').ModelCtor,
 * client: import('sequelize').ModelCtor,
 * clientToTeam: import('sequelize').ModelCtor,
 * comment: import('sequelize').ModelCtor,
 * facebookUser: import('sequelize').ModelCtor,
 * featureFlag: import('sequelize').ModelCtor,
 * filter: import('sequelize').ModelCtor,
 * googleCalendarUser: import('sequelize').ModelCtor,
 * googleUser: import('sequelize').ModelCtor,
 * inbox: import('sequelize').ModelCtor,
 * inboxActivity: import('sequelize').ModelCtor,
 * index: import('sequelize').ModelCtor,
 * log: import('sequelize').ModelCtor,
 * message: import('sequelize').ModelCtor,
 * messageStatus: import('sequelize').ModelCtor,
 * notification: import('sequelize').ModelCtor,
 * onboarding: import('sequelize').ModelCtor,
 * plan: import('sequelize').ModelCtor,
 * privateChat: import('sequelize').ModelCtor,
 * profile: import('sequelize').ModelCtor,
 * project: import('sequelize').ModelCtor,
 * reaction: import('sequelize').ModelCtor,
 * reactionToChatMessage: import('sequelize').ModelCtor,
 * reactionToMessage: import('sequelize').ModelCtor,
 * role: import('sequelize').ModelCtor,
 * setting: import('sequelize').ModelCtor,
 * shareLink: import('sequelize').ModelCtor,
 * stripeUser: import('sequelize').ModelCtor,
 * stripeWebhook: import('sequelize').ModelCtor,
 * subscription: import('sequelize').ModelCtor,
 * tagToTask: import('sequelize').ModelCtor,
 * task: import('sequelize').ModelCtor,
 * taskActivity: import('sequelize').ModelCtor,
 * taskRow: import('sequelize').ModelCtor,
 * taskTag: import('sequelize').ModelCtor,
 * taskToMember: import('sequelize').ModelCtor,
 * taskSubscription: import('sequelize').ModelCtor,
 * team: import('sequelize').ModelCtor,
 * teamAccess: import('sequelize').ModelCtor,
 * teamLogo: import('sequelize').ModelCtor,
 * teamMember: import('sequelize').ModelCtor,
 * teamRole: import('sequelize').ModelCtor,
 * teamVerification: import('sequelize').ModelCtor,
 * template: import('sequelize').ModelCtor,
 * templateAttachment: import('sequelize').ModelCtor,
 * templateOrder: import('sequelize').ModelCtor,
 * temporaryClient: import('sequelize').ModelCtor,
 * temporaryMember: import('sequelize').ModelCtor,
 * user: import('sequelize').ModelCtor
 * }}
 */
module.exports = require('../loaders/sequelize').sequelize.models;
